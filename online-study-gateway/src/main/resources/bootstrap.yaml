#微服务配置
spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.101.1:8848
      discovery:
        namespace: dev
        group: online-study-project
      config:
        namespace: dev
        group: online-study-project
        file-extension: yaml
        refresh-enabled: true